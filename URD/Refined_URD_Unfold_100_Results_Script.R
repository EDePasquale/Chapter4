load("~/Desktop/URD_Workspaces/URD_VanGalen_CHHi_100_results.RData")
tree = urd_object[["tree"]]
full_urd = urd_object[["full_urd"]]
uniqueGroups_original <- unique(full_urd@group.ids$group)
numclus = length(unique(uniqueGroups_original))
color_vec <- unique(uniqueGroups_original)
cluster_cols <- colorRampPalette(RColorBrewer::brewer.pal(name="Set1", n = 8))(numclus)
segment_cols <- colorRampPalette(RColorBrewer::brewer.pal(name="Set1", n = 8))(20)

tree = treeForceRotateCoords(tree,seg="27",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="27",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="3",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="3",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="3",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="3",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="30",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="30",x=3,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="30",x=-3,y=3,z=0)
tree = treeForceTranslateCoords(tree,seg="30",x=0,y=3,z=0)
tree = treeForceTranslateCoords(tree,seg="30",x=0,y=3,z=0)
tree = treeForceTranslateCoords(tree,seg="30",x=0,y=-3,z=0)
tree = treeForceRotateCoords(tree,seg="28",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="32",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="32",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="32",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="32",x=-3,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="1",x=0,y=5,z=0)
tree = treeForceTranslateCoords(tree,seg="9",x=0,y=5,z=0)
tree = treeForceRotateCoords(tree,seg="25",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="25",angle = pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="25",angle = pi/8,axis = "y",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="25",x=3,y=0,z=0)
tree = treeForceRotateCoords(tree,seg="30",angle = pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="30",angle = -pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="30",angle = -pi/10,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="22",angle = -pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="22",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="22",angle = pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="26",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="26",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="26",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="26",x=-3,y=3,z=0)
tree = treeForceTranslateCoords(tree,seg="26",x=3,y=-3,z=0)
tree = treeForceRotateCoords(tree,seg="23",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="23",angle = pi/2,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="25",angle = pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime",throw.out.cells = 15)
tree = treeForceRotateCoords(tree,seg="25",angle = -pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="25",x=-3,y=3,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=-3,y=3,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=6,y=-6,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=0,z=3)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=0,z=10)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=0,z=-2)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=0,z=-2)
tree = treeForceTranslateCoords(tree,seg="25",x=-10,y=0,z=0)
tree = treeForceRotateCoords(tree,seg="25",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="25",angle = -pi/4,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="25",x=5,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=-5,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=5,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=-5,z=-3)
tree = treeForceTranslateCoords(tree,seg="25",x=10,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=10,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=-5,y=0,z=0)
tree = treeForceTranslateCoords(tree,seg="25",x=-2,y=0,z=-2)
tree = treeForceTranslateCoords(tree,seg="25",x=,y=0,z=-2)
tree = treeForceTranslateCoords(tree,seg="25",x=0,y=0,z=-2)
tree = treeForceRotateCoords(tree,seg="25",angle = -pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="1",x=0,y=0,z=-5)
tree = treeForceTranslateCoords(tree,seg="1",x=0,y=0,z=-3)
tree = treeForceRotateCoords(tree,seg="1",angle = -pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceTranslateCoords(tree,seg="9",x=0,y=0,z=-5)
tree = treeForceTranslateCoords(tree,seg="9",x=0,y=0,z=-3)
tree = treeForceTranslateCoords(tree,seg="9",x=0,y=0,z=1)
tree = treeForceRotateCoords(tree,seg="32",angle = pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
tree = treeForceRotateCoords(tree,seg="32",angle = pi/8,axis = "z",around.cell = 10,pseudotime = "pseudotime")
plotTreeForce(tree,"segment",title = " ",alpha = 0.8,discrete.colors = segment_cols,label.tips = FALSE)
plotTreeForce(tree,"group",title = " ",alpha = 0.8,discrete.colors = cluster_cols,label.tips = FALSE)
save.image("~/Desktop/URD_Unfold_100_Results.RData")
